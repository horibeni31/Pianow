{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "mkdir build; cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && cmake --build ."
        }
    ]
}