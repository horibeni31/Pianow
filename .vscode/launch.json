{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "./build/Pianow",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}
